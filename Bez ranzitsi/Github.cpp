

#include <iostream>
#include <stdio.h>

int main(){
	int a = 1;
	int b = 2;
	int c = 3;
	int d = 4;
	int h = 3;
	if (h > (a + c))
		printf("Логическая ошибка");
	else
		printf("%d,%d,%d", P, S, l);
		
	int P = a + b + c + d;
	int S = (b + d) / h;
	int l = (b + d) / 2;
	
}
